# Components Directory

This folder hosts all shared UI building blocks for the typing test experience. Keep controls (toggles, panels, buttons), layout primitives, and animation-ready widgets here so the `app/` route files remain lean and declarative. When creating a new component, colocate its stories/tests and favor barrel exports for cleaner imports (`@/components`).
